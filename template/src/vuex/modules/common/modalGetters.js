// 模态窗口
export const modal = state => {
    return state.modal
}

export const modalEvent = state => {
    return state.modal.event
}

export
const modalQueryError = state => {
    return state.modal.error
}
